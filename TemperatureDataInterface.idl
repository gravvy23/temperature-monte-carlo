typedef float RowType[50];
typedef RowType TemperatureMesh[50];

interface TemperatureDataInterface
{
	float  randomWalk( 	inout TemperatureMesh mesh,
				in    unsigned long   row, in unsigned long column);
};
